#!/usr/bin/env bash
set -euo pipefail

go_files="$(git ls-files '*.go')"
if [ -n "$go_files" ]; then
  unformatted="$(gofmt -l $go_files)"
  if [ -n "$unformatted" ]; then
    printf 'gofmt required:\n%s\n' "$unformatted" >&2
    exit 1
  fi
fi

go test ./...
